// const script = document.getElementById("on-search-later");
// const search = script.getAttribute("data-search");

// if (search) {
//   const data = localStorage.getItem("searches");

//   let links = data ? JSON.parse(data) : [];
//   if (!Array.isArray(links)) {
//     links = [];
//   }

//   links.push(search);
//   localStorage.setItem("searches", JSON.stringify(links));
// }

// window.close();
